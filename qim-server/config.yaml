server:
  port: "8080"
  mode: "debug"

jwt:
  secret: "your-secret-key-change-in-production"
  expire_hours: 2
  refresh_expire_days: 7

database:
  type: "sqlite"
  path: "./qim.db"
  # type: "mysql"
  # host: "localhost"
  # port: 3306
  # username: "root"
  # password: "your-password"
  # database: "qim_server"
