# QIM 全栈优化与功能增强 - 验收检查清单

> 用于验证所有功能是否按 spec 要求正确实现。

## 阶段一：基础设施（P0）

### 分页与参数解析统一抽象
- [ ] `pkg/pagination/pagination.go` 已创建，包含 `Parse()` 和 `Middleware()` 函数
- [ ] `pkg/params/params.go` 已创建，包含 `GetUintParam()` 和 `MustGetUintParam()` 函数
- [ ] `pkg/response/response.go` 已添加 `SuccessWithPagination()` 方法
- [ ] 至少 3 个 handler 已使用新的分页/参数解析工具
- [ ] `go build ./...` 编译通过
- [ ] 分页参数边界值测试通过（page=0, page=-1, pageSize=0, pageSize=101）

### 分身设置回显修复
- [ ] 打开分身设置面板，已保存的配置正确显示
- [ ] 数据加载期间显示 loading 状态
- [ ] 配置修改后保存成功
- [ ] 刷新页面后配置仍然正确回显

---

## 阶段二：核心功能（P1）

### 群聊 @ 我特殊视觉提示
- [ ] 群聊中 @ 我的消息在聊天界面有特殊背景色
- [ ] 消息列表中包含未读 @ 消息的会话显示 "@" 徽标
- [ ] 聊天窗口顶部显示未读 @ 消息数量横幅
- [ ] 点击横幅自动滚动到第一条 @ 消息
- [ ] 后端消息返回数据包含 `is_at_mention` 字段
- [ ] 非 @ 消息不受影响

### 统一配置项管理
- [ ] `src/config/index.ts` 已创建，包含环境、API、分页、功能开关配置
- [ ] `request.ts` 使用配置中心的超时时间
- [ ] 列表页面使用配置中心的分页默认值
- [ ] 所有组件通过 `import { XXX } from '@/config'` 获取配置
- [ ] `npm run build` 构建通过

### 审计日志查询增强
- [ ] 支持时间范围筛选
- [ ] 支持操作类型筛选（增/删/改/查）
- [ ] 支持操作用户搜索
- [ ] 支持 IP 地址筛选
- [ ] 支持模块筛选
- [ ] 支持请求状态筛选（成功/失败）
- [ ] 点击日志行可展开详情面板
- [ ] 详情显示请求方法、URL、参数、响应、耗时
- [ ] 支持导出当前筛选结果为 CSV/Excel
- [ ] 后端支持新增筛选参数

### 操作日志自动记录 Middleware
- [ ] `middleware/operation_log.go` 已创建
- [ ] 所有认证 API 调用自动记录操作日志
- [ ] 日志包含用户 ID、用户名、操作类型、模块、IP、耗时、状态码
- [ ] 日志记录使用异步保存，不阻塞响应
- [ ] 健康检查等路径可配置跳过
- [ ] `routes.go` 已注册中间件
- [ ] `go build ./...` 编译通过

### 统一数据访问方式
- [ ] 所有 handler 通过 Service 层访问数据
- [ ] 无 `database.GetDB()` 或 `di.GlobalContainer.DB` 直接调用
- [ ] 新增 Service（blacklist、version 等）已创建
- [ ] `go build ./...` 编译通过

---

## 阶段三：体验增强（P1-P2）

### 官网式产品介绍首页
- [ ] 访问 `/home` 显示产品介绍页面
- [ ] 页面包含 Hero Section、核心功能展示、下载区域、功能点说明、Footer
- [ ] 无需登录即可访问
- [ ] 下载按钮链接到最新版本
- [ ] 显示最新版本号（从 API 获取）
- [ ] 路由配置 `meta: { requiresAuth: false }`
- [ ] 页面在不同屏幕尺寸下正常显示

### Request ID + Recovery Middleware
- [ ] `middleware/request_id.go` 已创建
- [ ] 每个请求生成唯一 Request ID
- [ ] 响应头包含 `X-Request-ID`
- [ ] `middleware/recovery.go` 已创建
- [ ] Panic 被捕获，返回友好错误响应
- [ ] Panic 堆栈被记录到日志
- [ ] `routes.go` 已注册中间件
- [ ] `go build ./...` 编译通过

### 请求限流 Middleware
- [ ] `middleware/rate_limit.go` 已创建
- [ ] 基于 IP 的限流生效（默认 100 req/min）
- [ ] 基于用户的限流生效（认证用户 200 req/min）
- [ ] 超过限制返回 429 Too Many Requests
- [ ] 限流配置可通过配置文件调整
- [ ] `go build ./...` 编译通过

---

## 代码质量检查

### 通用
- [ ] 所有新增文件包含适当的注释
- [ ] 代码风格与现有代码一致
- [ ] 无硬编码的配置值
- [ ] 错误处理完整且一致
- [ ] 无重复代码（DRY）

### 后端
- [ ] `go vet ./...` 通过
- [ ] `go test ./...` 通过（如有测试）
- [ ] 无未使用的导入
- [ ] 函数命名符合 Go 规范

### 前端
- [ ] `npm run lint` 通过
- [ ] `npm run typecheck` 通过（如有）
- [ ] 组件命名符合 Vue 规范
- [ ] 无未使用的变量/导入
- [ ] 响应式数据正确使用 ref/reactive

---

## 集成测试

- [ ] 前端与后端 API 对接正常
- [ ] 管理后台与客户端功能不冲突
- [ ] 所有新增路由可正常访问
- [ ] 所有新增中间件不影响现有功能
- [ ] 数据库迁移正确执行（如有）

---

## 文档

- [ ] API 文档已更新（如有新增/修改接口）
- [ ] 配置说明已更新（如有新增配置项）
- [ ] 部署说明已更新（如有新增中间件）

---

## 验收签名

| 检查项 | 检查人 | 日期 | 状态 |
|--------|--------|------|------|
| 阶段一 | | | |
| 阶段二 | | | |
| 阶段三 | | | |
| 代码质量 | | | |
| 集成测试 | | | |
