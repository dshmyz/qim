# QIM 全量回归测试案例列表

> 版本：1.0.0  
> 更新日期：2026-04-25  
> 适用范围：QIM 即时通讯系统（前端 + 后端）

---

## 目录

- [1. 认证模块](#1-认证模块)
- [2. 用户管理](#2-用户管理)
- [3. 会话管理](#3-会话管理)
- [4. 消息功能](#4-消息功能)
- [5. WebSocket 实时通信](#5-websocket-实时通信)
- [6. 组织架构](#6-组织架构)
- [7. 文件管理](#7-文件管理)
- [8. 笔记管理](#8-笔记管理)
- [9. 任务管理](#9-任务管理)
- [10. 日历事件](#10-日历事件)
- [11. 频道功能](#11-频道功能)
- [12. 通知系统](#12-通知系统)
- [13. 系统消息](#13-系统消息)
- [14. 应用管理](#14-应用管理)
- [15. 小程序管理](#15-小程序管理)
- [16. 短链接](#16-短链接)
- [17. AI 助手](#17-ai-助手)
- [18. 统计报表](#18-统计报表)
- [19. 前端界面交互](#19-前端界面交互)
- [20. Electron 桌面端](#20-electron-桌面端)
- [21. 安全与权限](#21-安全与权限)
- [22. 自动化 API 测试脚本](#22-自动化-api-测试脚本)

---

## 1. 认证模块

### 1.1 用户登录

| 字段 | 内容 |
|------|------|
| 测试编号 | AUTH-001 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 后端服务已启动，测试账号已创建

**测试步骤：**
1. POST `/api/v1/auth/login`，请求体：`{"username":"admin","password":"123456"}`
2. 验证响应 `code=0`，返回 token 和用户信息
3. 前端登录页面输入正确用户名密码，点击登录
4. 验证跳转到主界面，localStorage 中存储了 token 和 user

**预期结果：** 登录成功，返回有效 JWT token

---

### 1.2 登录失败 - 错误密码

| 字段 | 内容 |
|------|------|
| 测试编号 | AUTH-002 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. POST `/api/v1/auth/login`，请求体：`{"username":"admin","password":"wrong_password"}`

**预期结果：** 返回 `code=401`，message 为"用户名或密码错误"

---

### 1.3 登录失败 - 用户不存在

| 字段 | 内容 |
|------|------|
| 测试编号 | AUTH-003 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. POST `/api/v1/auth/login`，请求体：`{"username":"nonexistent","password":"123456"}`

**预期结果：** 返回 `code=401`

---

### 1.4 用户注册

| 字段 | 内容 |
|------|------|
| 测试编号 | AUTH-004 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. POST `/api/v1/auth/register`，请求体：`{"username":"testuser","password":"123456","nickname":"测试用户"}`
2. 验证返回 token 和用户信息

**预期结果：** 注册成功，返回有效 token

---

### 1.5 注册失败 - 用户名重复

| 字段 | 内容 |
|------|------|
| 测试编号 | AUTH-005 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. POST `/api/v1/auth/register`，使用已存在的用户名

**预期结果：** 返回错误码，提示用户名已存在

---

### 1.6 刷新 Token

| 字段 | 内容 |
|------|------|
| 测试编号 | AUTH-006 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 已有有效 token

**测试步骤：**
1. POST `/api/v1/auth/refresh`，Header：`Authorization: Bearer {token}`

**预期结果：** 返回新的 token

---

### 1.7 用户登出

| 字段 | 内容 |
|------|------|
| 测试编号 | AUTH-007 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 已有有效 token

**测试步骤：**
1. POST `/api/v1/auth/logout`，Header：`Authorization: Bearer {token}`

**预期结果：** 返回 `code=0`，message 为"登出成功"

---

### 1.8 双因素认证状态检查

| 字段 | 内容 |
|------|------|
| 测试编号 | AUTH-008 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. POST `/api/v1/auth/check-2fa`，请求体：`{"username":"admin"}`

**预期结果：** 返回 `twoFactorEnabled` 状态

---

### 1.9 双因素认证验证

| 字段 | 内容 |
|------|------|
| 测试编号 | AUTH-009 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**前置条件：** 用户已启用双因素认证

**测试步骤：**
1. 登录启用 2FA 的账号
2. 验证返回 `code=401, message="需要双因素认证"`，包含 session
3. POST `/api/v1/auth/2fa/verify`，传入 session、code、username

**预期结果：** 验证成功返回 token

---

### 1.10 登录 - 记住密码

| 字段 | 内容 |
|------|------|
| 测试编号 | AUTH-010 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P2 |

**测试步骤：**
1. 打开登录页面
2. 输入用户名密码，勾选"记住密码"
3. 点击登录
4. 关闭应用重新打开

**预期结果：** 用户名密码自动填充

---

### 1.11 登录 - 服务器地址配置

| 字段 | 内容 |
|------|------|
| 测试编号 | AUTH-011 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P2 |

**测试步骤：**
1. 打开登录页面，点击"设置服务器地址"
2. 修改服务器地址并保存
3. 重新打开登录页面

**预期结果：** 服务器地址保持上次设置的值

---

### 1.12 版本检查

| 字段 | 内容 |
|------|------|
| 测试编号 | AUTH-012 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. POST `/api/v1/auth/check-version`，请求体包含版本号

**预期结果：** 返回版本检查结果

---

## 2. 用户管理

### 2.1 获取当前用户信息

| 字段 | 内容 |
|------|------|
| 测试编号 | USER-001 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 已有有效 token

**测试步骤：**
1. GET `/api/v1/users/me`，Header：`Authorization: Bearer {token}`

**预期结果：** 返回当前用户完整信息，包含 id、username、nickname、avatar、signature 等

---

### 2.2 更新用户资料

| 字段 | 内容 |
|------|------|
| 测试编号 | USER-002 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 已有有效 token

**测试步骤：**
1. PUT `/api/v1/users/me`，请求体：`{"nickname":"新昵称","signature":"新签名"}`

**预期结果：** 返回更新后的用户信息

---

### 2.3 获取指定用户信息

| 字段 | 内容 |
|------|------|
| 测试编号 | USER-003 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 已有有效 token

**测试步骤：**
1. GET `/api/v1/users/:id`，Header：`Authorization: Bearer {token}`

**预期结果：** 返回目标用户信息

---

### 2.4 创建用户（管理员）

| 字段 | 内容 |
|------|------|
| 测试编号 | USER-004 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**前置条件：** 管理员权限 token

**测试步骤：**
1. POST `/api/v1/users`，请求体：`{"username":"newuser","password":"123456","nickname":"新用户"}`

**预期结果：** 返回创建的用户信息

---

### 2.5 搜索用户

| 字段 | 内容 |
|------|------|
| 测试编号 | USER-005 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 已有有效 token

**测试步骤：**
1. GET `/api/v1/users/search?q=关键词`

**预期结果：** 返回匹配的用户列表

---

## 3. 会话管理

### 3.1 获取会话列表

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-001 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 已有有效 token

**测试步骤：**
1. GET `/api/v1/conversations`

**预期结果：** 返回用户的所有会话列表，包含单聊、群聊、讨论组

---

### 3.2 创建单聊会话

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-002 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 已有有效 token，存在目标用户

**测试步骤：**
1. POST `/api/v1/conversations/single`，请求体：`{"user_id":2}`

**预期结果：** 返回创建的单聊会话信息

---

### 3.3 创建群聊会话

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-003 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 已有有效 token，存在多个用户

**测试步骤：**
1. POST `/api/v1/conversations/group`，请求体：`{"name":"测试群","member_ids":[2,3,4]}`

**预期结果：** 返回创建的群聊会话信息，创建者为 owner

---

### 3.4 创建讨论组会话

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-004 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 已有有效 token

**测试步骤：**
1. POST `/api/v1/conversations/discussion`，请求体：`{"name":"测试讨论组","member_ids":[2,3]}`

**预期结果：** 返回创建的讨论组会话信息

---

### 3.5 获取会话详情

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-005 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 已有有效 token，存在会话

**测试步骤：**
1. GET `/api/v1/conversations/:id`

**预期结果：** 返回会话详情，包含成员列表

---

### 3.6 会话置顶

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-006 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 已有有效 token，存在会话

**测试步骤：**
1. PUT `/api/v1/conversations/:id/pin`，请求体：`{"is_pinned":true}`
2. 获取会话列表，验证置顶会话排在前面

**预期结果：** 会话置顶成功

---

### 3.7 会话取消置顶

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-007 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. PUT `/api/v1/conversations/:id/pin`，请求体：`{"is_pinned":false}`

**预期结果：** 会话取消置顶成功

---

### 3.8 设置会话免打扰

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-008 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. PUT `/api/v1/conversations/:id/mute`，请求体：`{"muted":true}`

**预期结果：** 会话免打扰设置成功

---

### 3.9 解散群聊

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-009 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 群主身份

**测试步骤：**
1. DELETE `/api/v1/conversations/:id`

**预期结果：** 群聊解散成功

---

### 3.10 更新群聊信息

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-010 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 群主或管理员

**测试步骤：**
1. PUT `/api/v1/conversations/:id`，请求体：`{"name":"新群名"}`

**预期结果：** 群信息更新成功

---

### 3.11 添加群成员

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-011 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 已有有效 token，存在群聊

**测试步骤：**
1. POST `/api/v1/conversations/:id/members`，请求体：`{"user_ids":[5,6]}`

**预期结果：** 成员添加成功

---

### 3.12 移除群成员

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-012 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 群主或管理员

**测试步骤：**
1. DELETE `/api/v1/conversations/:id/members/:user_id`

**预期结果：** 成员移除成功

---

### 3.13 退出群聊

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-013 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 普通群成员

**测试步骤：**
1. POST `/api/v1/conversations/:id/exit`

**预期结果：** 退出群聊成功

---

### 3.14 设置管理员

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-014 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**前置条件：** 群主

**测试步骤：**
1. PUT `/api/v1/conversations/:id/members/:user_id/role`，请求体：`{"role":"admin"}`

**预期结果：** 角色设置成功

---

### 3.15 转让群主

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-015 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**前置条件：** 当前群主

**测试步骤：**
1. POST `/api/v1/conversations/:id/members/:user_id/transfer-owner`

**预期结果：** 群主转让成功

---

### 3.16 更新群公告

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-016 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 群主或管理员

**测试步骤：**
1. PUT `/api/v1/conversations/:id/announcement`，请求体：`{"announcement":"新公告内容"}`

**预期结果：** 群公告更新成功，聊天窗口显示公告

---

### 3.17 标记会话已读

| 字段 | 内容 |
|------|------|
| 测试编号 | CONV-017 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. POST `/api/v1/conversations/:id/read`

**预期结果：** 会话未读数清零

---

## 4. 消息功能

### 4.1 获取历史消息

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-001 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 已有有效 token，存在会话和消息

**测试步骤：**
1. GET `/api/v1/conversations/:id/messages?page=1&page_size=20`

**预期结果：** 返回消息列表，按时间倒序排列

---

### 4.2 发送文本消息

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-002 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 已有有效 token，存在会话

**测试步骤：**
1. POST `/api/v1/conversations/:id/messages`，请求体：`{"type":"text","content":"Hello World"}`

**预期结果：** 消息发送成功，返回消息对象

---

### 4.3 发送图片消息

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-003 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 已上传图片获取 URL

**测试步骤：**
1. POST `/api/v1/conversations/:id/messages`，请求体：`{"type":"image","content":"图片URL"}`

**预期结果：** 图片消息发送成功

---

### 4.4 发送文件消息

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-004 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 已上传文件获取 URL

**测试步骤：**
1. POST `/api/v1/conversations/:id/messages`，请求体：`{"type":"file","content":"文件URL","file_name":"test.pdf","file_size":1024}`

**预期结果：** 文件消息发送成功

---

### 4.5 发送引用消息

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-005 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 存在可引用的消息

**测试步骤：**
1. POST `/api/v1/conversations/:id/messages`，请求体：`{"type":"text","content":"回复内容","quoted_message_id":100}`

**预期结果：** 引用消息发送成功，返回包含引用的消息对象

---

### 4.6 发送分享消息

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-006 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. POST `/api/v1/conversations/:id/messages`，请求体包含 `share_data`

**预期结果：** 分享消息发送成功

---

### 4.7 发送小程序消息

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-007 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. POST `/api/v1/conversations/:id/messages`，请求体：`{"type":"miniApp","content":"小程序数据"}`

**预期结果：** 小程序消息发送成功

---

### 4.8 发送资讯消息

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-008 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. POST `/api/v1/conversations/:id/messages`，请求体：`{"type":"news","content":"资讯数据"}`

**预期结果：** 资讯消息发送成功

---

### 4.9 撤回消息

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-009 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 消息发送者在 2 分钟内

**测试步骤：**
1. POST `/api/v1/messages/:id/recall`

**预期结果：** 消息撤回成功，`is_recalled` 变为 true

---

### 4.10 撤回消息 - 超时

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-010 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**前置条件：** 消息发送超过 2 分钟

**测试步骤：**
1. POST `/api/v1/messages/:id/recall`

**预期结果：** 撤回失败，返回错误

---

### 4.11 删除消息

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-011 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. DELETE `/api/v1/messages/:id`

**预期结果：** 消息删除成功（软删除）

---

### 4.12 搜索消息

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-012 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. GET `/api/v1/messages/search?conversation_id=1&search=关键词`
2. 可按类型、日期筛选

**预期结果：** 返回匹配的消息列表

---

### 4.13 获取消息引用链

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-013 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. GET `/api/v1/messages/:id/quote-chain`

**预期结果：** 返回消息的引用链路

---

### 4.14 获取消息已读用户

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-014 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. GET `/api/v1/messages/:id/read-users`

**预期结果：** 返回已读用户列表

---

### 4.15 消息提醒

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-015 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. POST `/api/v1/messages/:id/remind`

**预期结果：** 消息提醒发送成功

---

### 4.16 流式消息

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-016 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. POST `/api/v1/conversations/:id/messages/stream`

**预期结果：** 流式消息发送成功

---

### 4.17 按条件筛选消息

| 字段 | 内容 |
|------|------|
| 测试编号 | MSG-017 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. GET `/api/v1/messages?conversation_id=1&type=text&sender_id=2`

**预期结果：** 返回符合筛选条件的消息

---

## 5. WebSocket 实时通信

### 5.1 WebSocket 连接

| 字段 | 内容 |
|------|------|
| 测试编号 | WS-001 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 已有有效 token

**测试步骤：**
1. 建立 WebSocket 连接：`ws://localhost:8080/api/v1/ws?token={token}`

**预期结果：** 连接成功，readyState 为 OPEN

---

### 5.2 WebSocket 心跳

| 字段 | 内容 |
|------|------|
| 测试编号 | WS-002 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** WebSocket 已连接

**测试步骤：**
1. 发送心跳消息：`{"type":"heartbeat","data":{}}`

**预期结果：** 连接保持正常

---

### 5.3 WebSocket 发送消息

| 字段 | 内容 |
|------|------|
| 测试编号 | WS-003 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** WebSocket 已连接

**测试步骤：**
1. 发送消息：`{"type":"send_message","data":{"conversation_id":1,"type":"text","content":"WebSocket消息"}}`

**预期结果：** 消息发送成功，接收方收到推送

---

### 5.4 WebSocket 标记消息已读

| 字段 | 内容 |
|------|------|
| 测试编号 | WS-004 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. 发送消息：`{"type":"read_message","data":{"conversation_id":1,"message_id":100}}`

**预期结果：** 消息标记已读成功

---

### 5.5 WebSocket 接收新消息推送

| 字段 | 内容 |
|------|------|
| 测试编号 | WS-005 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 两个客户端建立 WebSocket 连接

**测试步骤：**
1. 客户端 A 发送消息给客户端 B
2. 验证客户端 B 收到 `{"type":"new_message","data":{...}}` 推送

**预期结果：** B 实时收到消息推送

---

### 5.6 WebSocket 消息撤回推送

| 字段 | 内容 |
|------|------|
| 测试编号 | WS-006 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 两个客户端建立 WebSocket 连接，已有消息

**测试步骤：**
1. 撤回消息
2. 验证其他成员收到 `{"type":"message_recalled","data":{"message_id":xxx}}` 推送

**预期结果：** 实时收到撤回推送

---

### 5.7 WebSocket 用户状态推送

| 字段 | 内容 |
|------|------|
| 测试编号 | WS-007 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 两个客户端建立 WebSocket 连接

**测试步骤：**
1. 客户端 A 登出
2. 验证客户端 B 收到 `{"type":"user_status","data":{"user_id":A,"status":"offline"}}`

**预期结果：** 实时收到用户状态变更推送

---

### 5.8 WebSocket 断线重连

| 字段 | 内容 |
|------|------|
| 测试编号 | WS-008 |
| 测试类型 | 功能 |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. 断开网络连接
2. 恢复网络连接
3. 验证自动重连机制触发（3 秒间隔）

**预期结果：** 自动重新连接成功

---

### 5.9 WebSocket 消息确认 (ACK)

| 字段 | 内容 |
|------|------|
| 测试编号 | WS-009 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. 发送带 request_id 的消息
2. 验证收到 `{"type":"ack","data":{"request_id":"xxx","message_id":"yyy"}}`

**预期结果：** 收到消息确认

---

### 5.10 屏幕共享 WebSocket

| 字段 | 内容 |
|------|------|
| 测试编号 | WS-010 |
| 测试类型 | 功能 |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P2 |

**前置条件：** 已有有效 token

**测试步骤：**
1. 建立 WebSocket 连接：`ws://localhost:8080/api/v1/screen-share?token={token}`
2. 开始屏幕共享
3. 验证接收方能收到屏幕数据

**预期结果：** 屏幕共享功能正常

---

### 5.11 WebRTC 音视频通话

| 字段 | 内容 |
|------|------|
| 测试编号 | WS-011 |
| 测试类型 | 功能 |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P2 |

**前置条件：** 两个客户端在线

**测试步骤：**
1. 客户端 A 发起语音/视频通话
2. 验证 WebSocket 发送 `webrtc_offer`
3. 客户端 B 收到并回复 `webrtc_answer`
4. 双方交换 ICE candidate

**预期结果：** 通话建立成功

---

## 6. 组织架构

### 6.1 获取组织架构树

| 字段 | 内容 |
|------|------|
| 测试编号 | ORG-001 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 已有有效 token

**测试步骤：**
1. GET `/api/v1/organization/tree`

**预期结果：** 返回完整的树形组织结构，包含部门层级和员工信息

---

### 6.2 创建部门

| 字段 | 内容 |
|------|------|
| 测试编号 | ORG-002 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. POST `/api/v1/departments`，请求体：`{"name":"新部门","parent_id":1,"level":2}`

**预期结果：** 部门创建成功

---

### 6.3 关联用户到部门

| 字段 | 内容 |
|------|------|
| 测试编号 | ORG-003 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. POST `/api/v1/department-employees`，请求体：`{"user_id":1,"department_id":2,"position":"工程师","is_primary":true}`

**预期结果：** 关联成功

---

### 6.4 组织架构中发起私聊

| 字段 | 内容 |
|------|------|
| 测试编号 | ORG-004 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P1 |

**测试步骤：**
1. 在左侧栏切换到"组织架构"
2. 双击用户或点击"发起私聊"
3. 验证创建单聊会话

**预期结果：** 成功创建单聊并切换到聊天窗口

---

## 7. 文件管理

### 7.1 上传文件

| 字段 | 内容 |
|------|------|
| 测试编号 | FILE-001 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 已有有效 token

**测试步骤：**
1. POST `/api/v1/upload`，multipart/form-data，包含 file 字段

**预期结果：** 返回文件信息，包含 URL、文件名、大小等

---

### 7.2 获取文件列表

| 字段 | 内容 |
|------|------|
| 测试编号 | FILE-002 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. GET `/api/v1/files?page=1&page_size=20`

**预期结果：** 返回用户文件列表

---

### 7.3 下载文件

| 字段 | 内容 |
|------|------|
| 测试编号 | FILE-003 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 存在已上传文件

**测试步骤：**
1. GET `/api/v1/files/:id/download`

**预期结果：** 返回文件流，文件可正常下载

---

### 7.4 删除文件

| 字段 | 内容 |
|------|------|
| 测试编号 | FILE-004 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. DELETE `/api/v1/files/:id`

**预期结果：** 文件删除成功

---

### 7.5 创建文件夹

| 字段 | 内容 |
|------|------|
| 测试编号 | FILE-005 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. POST `/api/v1/folders`，请求体：`{"name":"新文件夹","parent_id":null}`

**预期结果：** 文件夹创建成功

---

### 7.6 获取文件夹树

| 字段 | 内容 |
|------|------|
| 测试编号 | FILE-006 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. GET `/api/v1/folders/tree`

**预期结果：** 返回完整的文件夹树结构

---

### 7.7 图片消息大图预览

| 字段 | 内容 |
|------|------|
| 测试编号 | FILE-007 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P1 |

**测试步骤：**
1. 在聊天窗口点击图片消息

**预期结果：** 弹出大图预览，可放大缩小关闭

---

### 7.8 文件消息另存为

| 字段 | 内容 |
|------|------|
| 测试编号 | FILE-008 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P2 |

**测试步骤：**
1. 在聊天窗口右键文件消息，选择"另存为"

**预期结果：** 弹出系统文件保存对话框

---

## 8. 笔记管理

### 8.1 获取笔记列表

| 字段 | 内容 |
|------|------|
| 测试编号 | NOTE-001 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 已有有效 token

**测试步骤：**
1. GET `/api/v1/notes`

**预期结果：** 返回用户笔记列表

---

### 8.2 创建笔记

| 字段 | 内容 |
|------|------|
| 测试编号 | NOTE-002 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**测试步骤：**
1. POST `/api/v1/notes`，请求体：`{"title":"新笔记","content":"# Markdown内容","color":"yellow"}`

**预期结果：** 笔记创建成功

---

### 8.3 获取笔记详情

| 字段 | 内容 |
|------|------|
| 测试编号 | NOTE-003 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. GET `/api/v1/notes/:id`

**预期结果：** 返回笔记完整信息

---

### 8.4 更新笔记

| 字段 | 内容 |
|------|------|
| 测试编号 | NOTE-004 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. PUT `/api/v1/notes/:id`，请求体：`{"title":"更新后的标题","content":"更新内容"}`

**预期结果：** 笔记更新成功

---

### 8.5 删除笔记

| 字段 | 内容 |
|------|------|
| 测试编号 | NOTE-005 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. DELETE `/api/v1/notes/:id`

**预期结果：** 笔记删除成功

---

### 8.6 笔记 Markdown 渲染

| 字段 | 内容 |
|------|------|
| 测试编号 | NOTE-006 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P1 |

**测试步骤：**
1. 打开笔记应用
2. 创建包含标题、列表、代码块的 Markdown 笔记

**预期结果：** Markdown 正确渲染

---

### 8.7 分享笔记到聊天

| 字段 | 内容 |
|------|------|
| 测试编号 | NOTE-007 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P2 |

**测试步骤：**
1. 在笔记中点击"分享"
2. 选择目标用户或群聊
3. 确认发送

**预期结果：** 聊天中收到分享消息卡片

---

## 9. 任务管理

### 9.1 获取任务列表

| 字段 | 内容 |
|------|------|
| 测试编号 | TASK-001 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 已有有效 token

**测试步骤：**
1. GET `/api/v1/tasks`

**预期结果：** 返回用户任务列表

---

### 9.2 创建任务

| 字段 | 内容 |
|------|------|
| 测试编号 | TASK-002 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**测试步骤：**
1. POST `/api/v1/tasks`，请求体：`{"title":"完成测试","description":"编写测试用例","priority":"high","due_date":"2026-04-30"}`

**预期结果：** 任务创建成功

---

### 9.3 更新任务

| 字段 | 内容 |
|------|------|
| 测试编号 | TASK-003 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. PUT `/api/v1/tasks/:id`，请求体：`{"title":"更新后的任务"}`

**预期结果：** 任务更新成功

---

### 9.4 更新任务状态

| 字段 | 内容 |
|------|------|
| 测试编号 | TASK-004 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. PATCH `/api/v1/tasks/:id/status`，请求体：`{"status":"completed"}`

**预期结果：** 任务状态更新成功（todo → in_progress → completed）

---

### 9.5 删除任务

| 字段 | 内容 |
|------|------|
| 测试编号 | TASK-005 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. DELETE `/api/v1/tasks/:id`

**预期结果：** 任务删除成功

---

### 9.6 任务看板视图

| 字段 | 内容 |
|------|------|
| 测试编号 | TASK-006 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P1 |

**测试步骤：**
1. 打开任务管理应用
2. 验证看板视图显示：待办、进行中、已完成三列

**预期结果：** 看板视图正确显示

---

## 10. 日历事件

### 10.1 获取日历事件列表

| 字段 | 内容 |
|------|------|
| 测试编号 | EVENT-001 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 已有有效 token

**测试步骤：**
1. GET `/api/v1/events`

**预期结果：** 返回用户日历事件列表

---

### 10.2 创建日历事件

| 字段 | 内容 |
|------|------|
| 测试编号 | EVENT-002 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**测试步骤：**
1. POST `/api/v1/events`，请求体：`{"title":"团队会议","description":"讨论进度","start":"2026-04-30T14:00:00Z","end":"2026-04-30T15:00:00Z","reminder":15}`

**预期结果：** 事件创建成功

---

### 10.3 获取事件详情

| 字段 | 内容 |
|------|------|
| 测试编号 | EVENT-003 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. GET `/api/v1/events/:id`

**预期结果：** 返回事件完整信息

---

### 10.4 更新日历事件

| 字段 | 内容 |
|------|------|
| 测试编号 | EVENT-004 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. PUT `/api/v1/events/:id`，请求体：`{"title":"更新后的会议"}`

**预期结果：** 事件更新成功

---

### 10.5 删除日历事件

| 字段 | 内容 |
|------|------|
| 测试编号 | EVENT-005 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. DELETE `/api/v1/events/:id`

**预期结果：** 事件删除成功

---

### 10.6 创建全天事件

| 字段 | 内容 |
|------|------|
| 测试编号 | EVENT-006 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. POST `/api/v1/events`，请求体：`{"title":"公司团建","start":"2026-04-30T00:00:00Z","end":"2026-04-30T23:59:59Z","all_day":true}`

**预期结果：** 全天事件创建成功

---

### 10.7 月视图日历展示

| 字段 | 内容 |
|------|------|
| 测试编号 | EVENT-007 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P1 |

**测试步骤：**
1. 打开日历应用
2. 验证月视图正确显示
3. 验证事件在对应日期显示

**预期结果：** 月视图正确渲染

---

## 11. 频道功能

### 11.1 创建频道（管理员）

| 字段 | 内容 |
|------|------|
| 测试编号 | CH-001 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**前置条件：** 系统管理员权限

**测试步骤：**
1. POST `/api/v1/channels`，请求体：`{"name":"公告频道","description":"公司公告"}`

**预期结果：** 频道创建成功

---

### 11.2 获取频道列表

| 字段 | 内容 |
|------|------|
| 测试编号 | CH-002 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. GET `/api/v1/channels`

**预期结果：** 返回频道列表

---

### 11.3 订阅频道

| 字段 | 内容 |
|------|------|
| 测试编号 | CH-003 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. POST `/api/v1/channels/:id/subscribe`

**预期结果：** 订阅成功

---

### 11.4 取消订阅频道

| 字段 | 内容 |
|------|------|
| 测试编号 | CH-004 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. POST `/api/v1/channels/:id/unsubscribe`

**预期结果：** 取消订阅成功

---

### 11.5 发布频道消息

| 字段 | 内容 |
|------|------|
| 测试编号 | CH-005 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 频道创建者

**测试步骤：**
1. POST `/api/v1/channels/:id/messages`，请求体：`{"content":"频道消息内容","type":"text"}`

**预期结果：** 频道消息发布成功

---

### 11.6 获取频道消息

| 字段 | 内容 |
|------|------|
| 测试编号 | CH-006 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. GET `/api/v1/channels/:id/messages`

**预期结果：** 返回频道消息列表

---

## 12. 通知系统

### 12.1 获取通知列表

| 字段 | 内容 |
|------|------|
| 测试编号 | NOTIF-001 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 已有有效 token

**测试步骤：**
1. GET `/api/v1/notifications?page=1&page_size=20`

**预期结果：** 返回通知列表，包含已读/未读状态

---

### 12.2 标记通知为已读

| 字段 | 内容 |
|------|------|
| 测试编号 | NOTIF-002 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. PUT `/api/v1/notifications/:id/read`

**预期结果：** 通知标记已读成功

---

### 12.3 标记所有通知为已读

| 字段 | 内容 |
|------|------|
| 测试编号 | NOTIF-003 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. PUT `/api/v1/notifications/read-all`

**预期结果：** 所有通知标记已读成功

---

### 12.4 通知中心未读数显示

| 字段 | 内容 |
|------|------|
| 测试编号 | NOTIF-004 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P1 |

**测试步骤：**
1. 发送新消息触发通知
2. 验证通知中心图标显示未读数徽章

**预期结果：** 未读数正确显示

---

## 13. 系统消息

### 13.1 获取系统消息

| 字段 | 内容 |
|------|------|
| 测试编号 | SYSMSG-001 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 已有有效 token

**测试步骤：**
1. GET `/api/v1/system-messages`

**预期结果：** 返回系统消息列表

---

### 13.2 发布系统消息（管理员）

| 字段 | 内容 |
|------|------|
| 测试编号 | SYSMSG-002 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** system_publisher 或 system_admin 角色

**测试步骤：**
1. POST `/api/v1/system-messages`，请求体：`{"title":"升级通知","content":"系统今晚升级","target":"all"}`

**预期结果：** 系统消息发布成功

---

### 13.3 发布系统消息到指定群组

| 字段 | 内容 |
|------|------|
| 测试编号 | SYSMSG-003 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. POST `/api/v1/system-messages`，请求体：`{"title":"群通知","content":"内容","target":"group","group_id":1}`

**预期结果：** 系统消息发布成功

---

### 13.4 发布系统消息到指定用户

| 字段 | 内容 |
|------|------|
| 测试编号 | SYSMSG-004 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. POST `/api/v1/system-messages`，请求体：`{"title":"个人通知","content":"内容","target":"user","user_id":1}`

**预期结果：** 系统消息发布成功

---

### 13.5 更新系统消息（超级管理员）

| 字段 | 内容 |
|------|------|
| 测试编号 | SYSMSG-005 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**前置条件：** system_admin 角色

**测试步骤：**
1. PUT `/api/v1/system-messages/:id`

**预期结果：** 系统消息更新成功

---

## 14. 应用管理

### 14.1 获取应用列表

| 字段 | 内容 |
|------|------|
| 测试编号 | APP-001 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**前置条件：** 已有有效 token

**测试步骤：**
1. GET `/api/v1/apps`

**预期结果：** 返回应用列表

---

### 14.2 获取所有应用

| 字段 | 内容 |
|------|------|
| 测试编号 | APP-002 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. GET `/api/v1/apps/all`

**预期结果：** 返回所有可用应用

---

### 14.3 创建应用

| 字段 | 内容 |
|------|------|
| 测试编号 | APP-003 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. POST `/api/v1/apps`，请求体：`{"name":"新应用","icon":"fas fa-star","category":"tools"}`

**预期结果：** 应用创建成功

---

### 14.4 更新应用

| 字段 | 内容 |
|------|------|
| 测试编号 | APP-004 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. PUT `/api/v1/apps/:id`，请求体：`{"name":"更新后的应用名"}`

**预期结果：** 应用更新成功

---

### 14.5 删除应用

| 字段 | 内容 |
|------|------|
| 测试编号 | APP-005 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. DELETE `/api/v1/apps/:id`

**预期结果：** 应用删除成功

---

### 14.6 统计报表展示

| 字段 | 内容 |
|------|------|
| 测试编号 | APP-006 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P1 |

**测试步骤：**
1. 打开应用中心，选择"统计报表"
2. 验证消息趋势、文件分布、任务完成率等可视化图表

**预期结果：** 报表正确渲染

---

### 14.7 获取统计数据

| 字段 | 内容 |
|------|------|
| 测试编号 | APP-007 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. GET `/api/v1/statistics`

**预期结果：** 返回统计数据

---

## 15. 小程序管理

### 15.1 获取小程序列表

| 字段 | 内容 |
|------|------|
| 测试编号 | MINIAPP-001 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. GET `/api/v1/mini-apps`

**预期结果：** 返回小程序列表

---

### 15.2 获取小程序详情

| 字段 | 内容 |
|------|------|
| 测试编号 | MINIAPP-002 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. GET `/api/v1/mini-apps/:id`

**预期结果：** 返回小程序详情

---

### 15.3 创建小程序

| 字段 | 内容 |
|------|------|
| 测试编号 | MINIAPP-003 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. POST `/api/v1/mini-apps`，请求体：`{"app_id":"mini_001","name":"测试小程序","description":"描述"}`

**预期结果：** 小程序创建成功

---

### 15.4 更新小程序

| 字段 | 内容 |
|------|------|
| 测试编号 | MINIAPP-004 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. PUT `/api/v1/mini-apps/:id`

**预期结果：** 小程序更新成功

---

### 15.5 删除小程序

| 字段 | 内容 |
|------|------|
| 测试编号 | MINIAPP-005 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. DELETE `/api/v1/mini-apps/:id`

**预期结果：** 小程序删除成功

---

### 15.6 聊天中发送小程序卡片

| 字段 | 内容 |
|------|------|
| 测试编号 | MINIAPP-006 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P2 |

**测试步骤：**
1. 在聊天输入区点击小程序按钮
2. 选择小程序
3. 验证小程序卡片发送成功

**预期结果：** 聊天中显示小程序卡片

---

## 16. 短链接

### 16.1 创建短链接

| 字段 | 内容 |
|------|------|
| 测试编号 | SL-001 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**前置条件：** 已有有效 token

**测试步骤：**
1. POST `/api/v1/shortlinks`，请求体：`{"original_url":"https://example.com/very/long/path"}`

**预期结果：** 返回短链接信息，包含唯一 code

---

### 16.2 获取短链接列表

| 字段 | 内容 |
|------|------|
| 测试编号 | SL-002 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. GET `/api/v1/shortlinks`

**预期结果：** 返回用户短链接列表，包含访问次数

---

### 16.3 短链接访问跳转

| 字段 | 内容 |
|------|------|
| 测试编号 | SL-003 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. GET `/:code`（不需要认证）

**预期结果：** 302 重定向到原始 URL，visit_count +1

---

## 17. AI 助手

### 17.1 获取 AI 配置

| 字段 | 内容 |
|------|------|
| 测试编号 | AI-001 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 已有有效 token

**测试步骤：**
1. GET `/api/v1/ai/config`

**预期结果：** 返回用户 AI 配置

---

### 17.2 更新 AI 配置

| 字段 | 内容 |
|------|------|
| 测试编号 | AI-002 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. PUT `/api/v1/ai/config`，请求体包含 provider、API Key、Model 等

**预期结果：** AI 配置更新成功

---

### 17.3 获取机器人列表

| 字段 | 内容 |
|------|------|
| 测试编号 | AI-003 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. GET `/api/v1/bots`

**预期结果：** 返回机器人列表

---

### 17.4 AI 对话（非流式）

| 字段 | 内容 |
|------|------|
| 测试编号 | AI-004 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** AI 配置已设置

**测试步骤：**
1. 向 AI 机器人发送消息

**预期结果：** 收到 AI 回复

---

### 17.5 AI 对话（流式）

| 字段 | 内容 |
|------|------|
| 测试编号 | AI-005 |
| 测试类型 | API + UI |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** AI 配置已设置

**测试步骤：**
1. 向 AI 机器人发送流式消息请求

**预期结果：** 流式返回 AI 回复内容

---

### 17.6 AI 助手 UI 交互

| 字段 | 内容 |
|------|------|
| 测试编号 | AI-006 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P1 |

**测试步骤：**
1. 打开应用中心，选择"AI 助手"
2. 选择机器人进行对话
3. 验证消息展示和流式输出

**预期结果：** AI 助手交互正常

---

### 17.7 多 AI 提供商支持

| 字段 | 内容 |
|------|------|
| 测试编号 | AI-007 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. 分别配置 OpenAI、Baidu、Alibaba、Tencent、Bytedance、Anthropic 并测试

**预期结果：** 各提供商均能正常调用

---

## 18. 统计报表

### 18.1 获取统计数据

| 字段 | 内容 |
|------|------|
| 测试编号 | STAT-001 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**前置条件：** 已有有效 token

**测试步骤：**
1. GET `/api/v1/statistics`

**预期结果：** 返回统计数据

---

### 18.2 统计可视化展示

| 字段 | 内容 |
|------|------|
| 测试编号 | STAT-002 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P1 |

**测试步骤：**
1. 打开统计报表应用
2. 验证消息趋势图、文件分布饼图、任务完成率等

**预期结果：** 图表正确渲染

---

## 19. 前端界面交互

### 19.1 侧边栏切换

| 字段 | 内容 |
|------|------|
| 测试编号 | UI-001 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P0 |

**测试步骤：**
1. 依次点击：最近联系人 → 组织架构 → 群聊 → 应用 → 更多
2. 验证每个面板正确切换

**预期结果：** 面板切换流畅，内容正确

---

### 19.2 会话搜索

| 字段 | 内容 |
|------|------|
| 测试编号 | UI-002 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P1 |

**测试步骤：**
1. 在搜索框输入关键词
2. 验证搜索结果列表实时更新

**预期结果：** 搜索结果正确显示

---

### 19.3 消息右键菜单

| 字段 | 内容 |
|------|------|
| 测试编号 | UI-003 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P1 |

**测试步骤：**
1. 右键消息
2. 验证菜单选项：引用、回复、撤回、删除、复制等

**预期结果：** 右键菜单正常显示，功能可用

---

### 19.4 会话右键菜单

| 字段 | 内容 |
|------|------|
| 测试编号 | UI-004 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P1 |

**测试步骤：**
1. 右键会话列表中的会话
2. 验证菜单选项：置顶、免打扰、移除等

**预期结果：** 右键菜单正常显示

---

### 19.5 表情面板

| 字段 | 内容 |
|------|------|
| 测试编号 | UI-005 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P2 |

**测试步骤：**
1. 点击表情按钮
2. 选择表情插入输入框
3. 发送含表情的消息

**预期结果：** 表情正确插入并发送

---

### 19.6 截图功能

| 字段 | 内容 |
|------|------|
| 测试编号 | UI-006 |
| 测试类型 | UI (Electron) |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P1 |

**前置条件：** Electron 桌面端

**测试步骤：**
1. 点击截图按钮
2. 框选截图区域
3. 发送截图

**预期结果：** 截图功能正常

---

### 19.7 @成员功能

| 字段 | 内容 |
|------|------|
| 测试编号 | UI-007 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P1 |

**前置条件：** 群聊/讨论组

**测试步骤：**
1. 输入框输入 @
2. 验证弹出成员选择面板
3. 选择成员后继续输入

**预期结果：** @成员功能正常

---

### 19.8 消息加载更多

| 字段 | 内容 |
|------|------|
| 测试编号 | UI-008 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P1 |

**测试步骤：**
1. 在聊天窗口滚动到顶部
2. 验证自动加载更早的消息

**预期结果：** 分页加载更多消息

---

### 19.9 皮肤主题切换

| 字段 | 内容 |
|------|------|
| 测试编号 | UI-009 |
| 测试类型 | UI |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P2 |

**测试步骤：**
1. 点击皮肤按钮
2. 选择不同主题

**预期结果：** 主题切换生效

---

### 20.1 窗口最小化

| 字段 | 内容 |
|------|------|
| 测试编号 | EL-001 |
| 测试类型 | UI (Electron) |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P2 |

**前置条件：** Electron 桌面端

**测试步骤：**
1. 点击窗口最小化按钮

**预期结果：** 窗口最小化到任务栏

---

### 20.2 窗口最大化/还原

| 字段 | 内容 |
|------|------|
| 测试编号 | EL-002 |
| 测试类型 | UI (Electron) |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P2 |

**前置条件：** Electron 桌面端

**测试步骤：**
1. 点击最大化按钮
2. 点击还原按钮

**预期结果：** 窗口正常最大化/还原

---

### 20.3 窗口关闭

| 字段 | 内容 |
|------|------|
| 测试编号 | EL-003 |
| 测试类型 | UI (Electron) |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P2 |

**前置条件：** Electron 桌面端

**测试步骤：**
1. 点击关闭按钮

**预期结果：** 窗口关闭

---

### 20.4 版本更新检测

| 字段 | 内容 |
|------|------|
| 测试编号 | EL-004 |
| 测试类型 | UI (Electron) |
| 自动化 | ⚠️ 需 E2E 工具 |
| 优先级 | P2 |

**前置条件：** Electron 桌面端

**测试步骤：**
1. 启动应用
2. 验证版本更新检测逻辑

**预期结果：** 有新版本时弹出更新提示

---

### 20.5 多平台打包

| 字段 | 内容 |
|------|------|
| 测试编号 | EL-005 |
| 测试类型 | 构建 |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. 运行 `npm run build` 构建前端资源
2. 运行 `npm run electron:build` 打包桌面应用

**预期结果：** 构建成功，输出应用在 electron-dist 目录

---

## 21. 安全与权限

### 21.1 未认证访问受保护路由

| 字段 | 内容 |
|------|------|
| 测试编号 | SEC-001 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**测试步骤：**
1. GET `/api/v1/users/me`（不带 Authorization header）

**预期结果：** 返回 401 未认证

---

### 21.2 无效 Token 访问

| 字段 | 内容 |
|------|------|
| 测试编号 | SEC-002 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P0 |

**测试步骤：**
1. GET `/api/v1/users/me`，Header：`Authorization: Bearer invalid_token`

**预期结果：** 返回 401

---

### 21.3 管理员权限验证 - 创建频道

| 字段 | 内容 |
|------|------|
| 测试编号 | SEC-003 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. 使用普通用户 token 尝试 POST `/api/v1/channels`

**预期结果：** 返回 403 无权限

---

### 21.4 管理员权限验证 - 发布系统消息

| 字段 | 内容 |
|------|------|
| 测试编号 | SEC-004 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. 使用普通用户 token 尝试 POST `/api/v1/system-messages`

**预期结果：** 返回 403 无权限

---

### 21.5 角色管理 - 添加用户角色

| 字段 | 内容 |
|------|------|
| 测试编号 | SEC-005 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**前置条件：** system_admin 角色

**测试步骤：**
1. POST `/api/v1/users/:id/roles`，请求体：`{"role":"system_publisher"}`

**预期结果：** 角色添加成功

---

### 21.6 角色管理 - 移除用户角色

| 字段 | 内容 |
|------|------|
| 测试编号 | SEC-006 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**前置条件：** system_admin 角色

**测试步骤：**
1. DELETE `/api/v1/users/:id/roles/:role`

**预期结果：** 角色移除成功

---

### 21.7 跨域资源共享 (CORS)

| 字段 | 内容 |
|------|------|
| 测试编号 | SEC-007 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P1 |

**测试步骤：**
1. 发送 OPTIONS 请求到任意 API 端点
2. 验证响应头包含 `Access-Control-Allow-Origin: *`

**预期结果：** CORS 配置正确

---

### 21.8 静态文件访问安全

| 字段 | 内容 |
|------|------|
| 测试编号 | SEC-008 |
| 测试类型 | API |
| 自动化 | ✅ 可自动化 |
| 优先级 | P2 |

**测试步骤：**
1. GET `/uploads/...` 尝试访问不存在的文件
2. 尝试路径穿越攻击

**预期结果：** 404 或拒绝访问

---

## 22. 自动化 API 测试脚本

以下为可直接执行的 API 自动化测试脚本。基于 curl + bash，无需额外依赖。

### 22.1 环境准备

```bash
#!/bin/bash
# test-env-setup.sh
# 测试环境配置

BASE_URL="${QIM_BASE_URL:-http://localhost:8080}"
echo "测试服务器: $BASE_URL"

# 检查服务器是否运行
if ! curl -s "$BASE_URL/api/v1/auth/login" -X POST -H "Content-Type: application/json" -d '{"username":"test","password":"test"}' > /dev/null 2>&1; then
    echo "❌ 服务器未启动，请先运行: cd qim-server && go run main.go"
    exit 1
fi
echo "✅ 服务器运行正常"
```

### 22.2 认证模块测试

```bash
#!/bin/bash
# test-auth.sh
# 认证模块自动化测试

BASE_URL="${QIM_BASE_URL:-http://localhost:8080}"
PASS=0
FAIL=0

# 工具函数
assert_eq() {
    local expected=$1 actual=$2 test_name=$3
    if [ "$expected" = "$actual" ]; then
        echo "✅ PASS: $test_name"
        PASS=$((PASS+1))
    else
        echo "❌ FAIL: $test_name (期望=$expected, 实际=$actual)"
        FAIL=$((FAIL+1))
    fi
}

echo "===== 认证模块测试 ====="

# AUTH-001: 用户登录
echo "--- AUTH-001: 用户登录 ---"
LOGIN_RESP=$(curl -s -X POST "$BASE_URL/api/v1/auth/login" \
    -H "Content-Type: application/json" \
    -d '{"username":"admin","password":"123456"}')
CODE=$(echo "$LOGIN_RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "用户登录"

# 提取 Token
TOKEN=$(echo "$LOGIN_RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['data']['token'])" 2>/dev/null)

# AUTH-002: 错误密码登录
echo "--- AUTH-002: 错误密码登录 ---"
LOGIN_FAIL_RESP=$(curl -s -X POST "$BASE_URL/api/v1/auth/login" \
    -H "Content-Type: application/json" \
    -d '{"username":"admin","password":"wrong"}')
CODE=$(echo "$LOGIN_FAIL_RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "401" "$CODE" "错误密码登录返回401"

# AUTH-003: 用户不存在
echo "--- AUTH-003: 用户不存在 ---"
LOGIN_NORESP=$(curl -s -X POST "$BASE_URL/api/v1/auth/login" \
    -H "Content-Type: application/json" \
    -d '{"username":"nonexistent","password":"123"}')
CODE=$(echo "$LOGIN_NORESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "401" "$CODE" "用户不存在返回401"

# AUTH-004: 用户注册
echo "--- AUTH-004: 用户注册 ---"
REG_RESP=$(curl -s -X POST "$BASE_URL/api/v1/auth/register" \
    -H "Content-Type: application/json" \
    -d '{"username":"test_'"$(date +%s)"'","password":"123456","nickname":"测试用户"}')
CODE=$(echo "$REG_RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "用户注册"

# AUTH-006: 刷新 Token
echo "--- AUTH-006: 刷新Token ---"
REFRESH_RESP=$(curl -s -X POST "$BASE_URL/api/v1/auth/refresh" \
    -H "Authorization: Bearer $TOKEN")
CODE=$(echo "$REFRESH_RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "刷新Token"

# AUTH-007: 用户登出
echo "--- AUTH-007: 用户登出 ---"
LOGOUT_RESP=$(curl -s -X POST "$BASE_URL/api/v1/auth/logout" \
    -H "Authorization: Bearer $TOKEN")
CODE=$(echo "$LOGOUT_RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "用户登出"

echo ""
echo "===== 认证测试结果: $PASS 通过, $FAIL 失败 ====="
```

### 22.3 用户管理测试

```bash
#!/bin/bash
# test-users.sh
# 用户管理自动化测试

BASE_URL="${QIM_BASE_URL:-http://localhost:8080}"
TOKEN="${QIM_TOKEN:-}"
PASS=0
FAIL=0

assert_eq() {
    local expected=$1 actual=$2 test_name=$3
    if [ "$expected" = "$actual" ]; then
        echo "✅ PASS: $test_name"
        PASS=$((PASS+1))
    else
        echo "❌ FAIL: $test_name (期望=$expected, 实际=$actual)"
        FAIL=$((FAIL+1))
    fi
}

echo "===== 用户管理测试 ====="

# USER-001: 获取当前用户
echo "--- USER-001: 获取当前用户 ---"
RESP=$(curl -s "$BASE_URL/api/v1/users/me" -H "Authorization: Bearer $TOKEN")
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "获取当前用户"

# USER-002: 更新用户资料
echo "--- USER-002: 更新用户资料 ---"
RESP=$(curl -s -X PUT "$BASE_URL/api/v1/users/me" \
    -H "Authorization: Bearer $TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"nickname":"测试昵称","signature":"测试签名"}')
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "更新用户资料"

echo ""
echo "===== 用户管理测试结果: $PASS 通过, $FAIL 失败 ====="
```

### 22.4 完整回归测试套件

```bash
#!/bin/bash
# test-all.sh
# QIM 完整回归测试套件
# 用法: QIM_BASE_URL=http://localhost:8080 bash test-all.sh

BASE_URL="${QIM_BASE_URL:-http://localhost:8080}"
TOTAL_PASS=0
TOTAL_FAIL=0

# 工具函数
assert_eq() {
    local expected=$1 actual=$2 test_name=$3
    if [ "$expected" = "$actual" ]; then
        echo "  ✅ PASS: $test_name"
        TOTAL_PASS=$((TOTAL_PASS+1))
    else
        echo "  ❌ FAIL: $test_name (期望=$expected, 实际=$actual)"
        TOTAL_FAIL=$((TOTAL_FAIL+1))
    fi
}

check_server() {
    echo "检查服务器状态..."
    if curl -s "$BASE_URL" > /dev/null 2>&1; then
        echo "✅ 服务器运行正常"
    else
        echo "❌ 服务器未启动，请先运行: cd qim-server && go run main.go"
        exit 1
    fi
}

echo "========================================"
echo "  QIM 全量回归测试"
echo "  服务器: $BASE_URL"
echo "  日期: $(date '+%Y-%m-%d %H:%M:%S')"
echo "========================================"
echo ""

check_server

# ===== 1. 认证测试 =====
echo ">>> [1/15] 认证模块测试"

# 登录获取 Token
LOGIN_RESP=$(curl -s -X POST "$BASE_URL/api/v1/auth/login" \
    -H "Content-Type: application/json" \
    -d '{"username":"admin","password":"123456"}')
TOKEN=$(echo "$LOGIN_RESP" | python3 -c "import sys,json;d=json.load(sys.stdin);print(d.get('data',{}).get('token',''))" 2>/dev/null)
USER_ID=$(echo "$LOGIN_RESP" | python3 -c "import sys,json;d=json.load(sys.stdin);print(d.get('data',{}).get('user',{}).get('id',''))" 2>/dev/null)

if [ -n "$TOKEN" ]; then
    assert_eq "非空" "非空" "登录成功获取Token"
else
    echo "  ⚠️ 跳过认证测试（登录失败或无admin账号）"
    echo "  提示: 请先注册用户: curl -X POST $BASE_URL/api/v1/auth/register -H 'Content-Type: application/json' -d '{\"username\":\"admin\",\"password\":\"123456\",\"nickname\":\"管理员\"}'"
    exit 1
fi

# ===== 2. 用户管理 =====
echo ""
echo ">>> [2/15] 用户管理测试"

RESP=$(curl -s "$BASE_URL/api/v1/users/me" -H "Authorization: Bearer $TOKEN")
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "获取当前用户"

RESP=$(curl -s -X PUT "$BASE_URL/api/v1/users/me" \
    -H "Authorization: Bearer $TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"nickname":"回归测试用户"}')
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "更新用户资料"

RESP=$(curl -s "$BASE_URL/api/v1/users/search?keyword=admin" -H "Authorization: Bearer $TOKEN")
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "搜索用户"

# ===== 3. 会话管理 =====
echo ""
echo ">>> [3/15] 会话管理测试"

# 创建单聊
RESP=$(curl -s -X POST "$BASE_URL/api/v1/conversations/single" \
    -H "Authorization: Bearer $TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"user_id":2}' 2>/dev/null)
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin).get('code',''))" 2>/dev/null)
assert_eq "0" "$CODE" "创建单聊会话"

# 创建群聊
GROUP_RESP=$(curl -s -X POST "$BASE_URL/api/v1/conversations/group" \
    -H "Authorization: Bearer $TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"name":"回归测试群","member_ids":[2,3]}' 2>/dev/null)
GROUP_ID=$(echo "$GROUP_RESP" | python3 -c "import sys,json;d=json.load(sys.stdin);print(d.get('data',{}).get('id',''))" 2>/dev/null)
CODE=$(echo "$GROUP_RESP" | python3 -c "import sys,json;print(json.load(sys.stdin).get('code',''))" 2>/dev/null)
assert_eq "0" "$CODE" "创建群聊会话"

# 获取会话列表
RESP=$(curl -s "$BASE_URL/api/v1/conversations" -H "Authorization: Bearer $TOKEN")
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "获取会话列表"

# 会话置顶（如果群聊创建成功）
if [ -n "$GROUP_ID" ]; then
    RESP=$(curl -s -X PUT "$BASE_URL/api/v1/conversations/$GROUP_ID/pin" \
        -H "Authorization: Bearer $TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"is_pinned":true}')
    CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
    assert_eq "0" "$CODE" "会话置顶"
fi

# 获取会话详情
if [ -n "$GROUP_ID" ]; then
    RESP=$(curl -s "$BASE_URL/api/v1/conversations/$GROUP_ID" -H "Authorization: Bearer $TOKEN")
    CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
    assert_eq "0" "$CODE" "获取会话详情"
fi

# ===== 4. 消息功能 =====
echo ""
echo ">>> [4/15] 消息功能测试"

if [ -n "$GROUP_ID" ]; then
    # 发送文本消息
    MSG_RESP=$(curl -s -X POST "$BASE_URL/api/v1/conversations/$GROUP_ID/messages" \
        -H "Authorization: Bearer $TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"type":"text","content":"回归测试消息"}')
    MSG_ID=$(echo "$MSG_RESP" | python3 -c "import sys,json;d=json.load(sys.stdin);print(d.get('data',{}).get('id',''))" 2>/dev/null)
    CODE=$(echo "$MSG_RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
    assert_eq "0" "$CODE" "发送文本消息"

    # 获取历史消息
    RESP=$(curl -s "$BASE_URL/api/v1/conversations/$GROUP_ID/messages" -H "Authorization: Bearer $TOKEN")
    CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
    assert_eq "0" "$CODE" "获取历史消息"

    # 撤回消息
    if [ -n "$MSG_ID" ]; then
        RESP=$(curl -s -X POST "$BASE_URL/api/v1/messages/$MSG_ID/recall" \
            -H "Authorization: Bearer $TOKEN")
        CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
        assert_eq "0" "$CODE" "撤回消息"
    fi
fi

# ===== 5. 组织架构 =====
echo ""
echo ">>> [5/15] 组织架构测试"

RESP=$(curl -s "$BASE_URL/api/v1/organization/tree" -H "Authorization: Bearer $TOKEN")
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "获取组织架构树"

# ===== 6. 文件管理 =====
echo ""
echo ">>> [6/15] 文件管理测试"

# 创建测试文件
echo "test content" > /tmp/qim_test_file.txt
RESP=$(curl -s -X POST "$BASE_URL/api/v1/upload" \
    -H "Authorization: Bearer $TOKEN" \
    -F "file=@/tmp/qim_test_file.txt")
FILE_ID=$(echo "$RESP" | python3 -c "import sys,json;d=json.load(sys.stdin);print(d.get('data',{}).get('id',''))" 2>/dev/null)
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "上传文件"

# 获取文件列表
RESP=$(curl -s "$BASE_URL/api/v1/files" -H "Authorization: Bearer $TOKEN")
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "获取文件列表"

# 删除文件
if [ -n "$FILE_ID" ]; then
    RESP=$(curl -s -X DELETE "$BASE_URL/api/v1/files/$FILE_ID" -H "Authorization: Bearer $TOKEN")
    CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
    assert_eq "0" "$CODE" "删除文件"
fi

rm -f /tmp/qim_test_file.txt

# ===== 7. 笔记管理 =====
echo ""
echo ">>> [7/15] 笔记管理测试"

NOTE_RESP=$(curl -s -X POST "$BASE_URL/api/v1/notes" \
    -H "Authorization: Bearer $TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"title":"回归测试笔记","content":"# 测试内容","color":"blue"}')
NOTE_ID=$(echo "$NOTE_RESP" | python3 -c "import sys,json;d=json.load(sys.stdin);print(d.get('data',{}).get('id',''))" 2>/dev/null)
CODE=$(echo "$NOTE_RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "创建笔记"

if [ -n "$NOTE_ID" ]; then
    # 获取笔记列表
    RESP=$(curl -s "$BASE_URL/api/v1/notes" -H "Authorization: Bearer $TOKEN")
    CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
    assert_eq "0" "$CODE" "获取笔记列表"

    # 更新笔记
    RESP=$(curl -s -X PUT "$BASE_URL/api/v1/notes/$NOTE_ID" \
        -H "Authorization: Bearer $TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"title":"更新后的笔记"}')
    CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
    assert_eq "0" "$CODE" "更新笔记"

    # 删除笔记
    RESP=$(curl -s -X DELETE "$BASE_URL/api/v1/notes/$NOTE_ID" -H "Authorization: Bearer $TOKEN")
    CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
    assert_eq "0" "$CODE" "删除笔记"
fi

# ===== 8. 任务管理 =====
echo ""
echo ">>> [8/15] 任务管理测试"

TASK_RESP=$(curl -s -X POST "$BASE_URL/api/v1/tasks" \
    -H "Authorization: Bearer $TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"title":"回归测试任务","description":"测试描述","priority":"high"}')
TASK_ID=$(echo "$TASK_RESP" | python3 -c "import sys,json;d=json.load(sys.stdin);print(d.get('data',{}).get('id',''))" 2>/dev/null)
CODE=$(echo "$TASK_RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "创建任务"

if [ -n "$TASK_ID" ]; then
    # 获取任务列表
    RESP=$(curl -s "$BASE_URL/api/v1/tasks" -H "Authorization: Bearer $TOKEN")
    CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
    assert_eq "0" "$CODE" "获取任务列表"

    # 更新任务状态
    RESP=$(curl -s -X PATCH "$BASE_URL/api/v1/tasks/$TASK_ID/status" \
        -H "Authorization: Bearer $TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"status":"completed"}')
    CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
    assert_eq "0" "$CODE" "更新任务状态"

    # 删除任务
    RESP=$(curl -s -X DELETE "$BASE_URL/api/v1/tasks/$TASK_ID" -H "Authorization: Bearer $TOKEN")
    CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
    assert_eq "0" "$CODE" "删除任务"
fi

# ===== 9. 日历事件 =====
echo ""
echo ">>> [9/15] 日历事件测试"

EVENT_RESP=$(curl -s -X POST "$BASE_URL/api/v1/events" \
    -H "Authorization: Bearer $TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"title":"回归测试事件","description":"测试","start":"2026-05-01T10:00:00Z","end":"2026-05-01T11:00:00Z","reminder":15}')
EVENT_ID=$(echo "$EVENT_RESP" | python3 -c "import sys,json;d=json.load(sys.stdin);print(d.get('data',{}).get('id',''))" 2>/dev/null)
CODE=$(echo "$EVENT_RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "创建日历事件"

if [ -n "$EVENT_ID" ]; then
    RESP=$(curl -s "$BASE_URL/api/v1/events" -H "Authorization: Bearer $TOKEN")
    CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
    assert_eq "0" "$CODE" "获取事件列表"

    RESP=$(curl -s -X DELETE "$BASE_URL/api/v1/events/$EVENT_ID" -H "Authorization: Bearer $TOKEN")
    CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
    assert_eq "0" "$CODE" "删除日历事件"
fi

# ===== 10. 应用管理 =====
echo ""
echo ">>> [10/15] 应用管理测试"

RESP=$(curl -s "$BASE_URL/api/v1/apps" -H "Authorization: Bearer $TOKEN")
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "获取应用列表"

RESP=$(curl -s "$BASE_URL/api/v1/apps/all" -H "Authorization: Bearer $TOKEN")
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "获取所有应用"

# ===== 11. 小程序管理 =====
echo ""
echo ">>> [11/15] 小程序管理测试"

RESP=$(curl -s "$BASE_URL/api/v1/mini-apps" -H "Authorization: Bearer $TOKEN")
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "获取小程序列表"

# ===== 12. 通知管理 =====
echo ""
echo ">>> [12/15] 通知管理测试"

RESP=$(curl -s "$BASE_URL/api/v1/notifications" -H "Authorization: Bearer $TOKEN")
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "获取通知列表"

RESP=$(curl -s -X PUT "$BASE_URL/api/v1/notifications/read-all" -H "Authorization: Bearer $TOKEN")
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "标记所有通知已读"

# ===== 13. 频道管理 =====
echo ""
echo ">>> [13/15] 频道管理测试"

RESP=$(curl -s "$BASE_URL/api/v1/channels" -H "Authorization: Bearer $TOKEN")
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "获取频道列表"

# ===== 14. 系统消息 =====
echo ""
echo ">>> [14/15] 系统消息测试"

RESP=$(curl -s "$BASE_URL/api/v1/system-messages" -H "Authorization: Bearer $TOKEN")
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "0" "$CODE" "获取系统消息"

# ===== 15. 安全测试 =====
echo ""
echo ">>> [15/15] 安全测试"

# 未认证访问
RESP=$(curl -s -o /dev/null -w "%{http_code}" "$BASE_URL/api/v1/users/me")
assert_eq "401" "$RESP" "未认证返回401"

# 无效 Token
RESP=$(curl -s "$BASE_URL/api/v1/users/me" -H "Authorization: Bearer invalid_token")
CODE=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['code'])" 2>/dev/null)
assert_eq "401" "$CODE" "无效Token返回401"

# CORS
RESP_HEADERS=$(curl -s -I -X OPTIONS "$BASE_URL/api/v1/users/me" \
    -H "Origin: http://localhost:5173" \
    -H "Access-Control-Request-Method: GET" 2>/dev/null)
if echo "$RESP_HEADERS" | grep -qi "access-control-allow-origin"; then
    echo "  ✅ PASS: CORS配置正确"
    TOTAL_PASS=$((TOTAL_PASS+1))
else
    echo "  ❌ FAIL: CORS配置缺失"
    TOTAL_FAIL=$((TOTAL_FAIL+1))
fi

# ===== 测试结果汇总 =====
echo ""
echo "========================================"
echo "  测试完成"
echo "  通过: $TOTAL_PASS"
echo "  失败: $TOTAL_FAIL"
echo "  总计: $((TOTAL_PASS + TOTAL_FAIL))"
echo "  成功率: $(echo "scale=1; $TOTAL_PASS * 100 / ($TOTAL_PASS + $TOTAL_FAIL)" | bc 2>/dev/null || echo "N/A")%"
echo "========================================"

if [ "$TOTAL_FAIL" -gt 0 ]; then
    exit 1
fi
exit 0
```

### 22.7 WebSocket 自动化测试

```bash
#!/usr/bin/env python3
# test-websocket.py
# WebSocket 功能自动化测试
# 用法: QIM_TOKEN=<token> python3 test-websocket.py

import asyncio
import json
import os
import sys
import websockets

BASE_URL = os.environ.get("QIM_BASE_URL", "ws://localhost:8080")
TOKEN = os.environ.get("QIM_TOKEN", "")

if not TOKEN:
    print("❌ 请设置 QIM_TOKEN 环境变量")
    print("   示例: QIM_TOKEN=<your_token> python3 test-websocket.py")
    sys.exit(1)

PASS = 0
FAIL = 0

def assert_result(condition, test_name):
    global PASS, FAIL
    if condition:
        print(f"  ✅ PASS: {test_name}")
        PASS += 1
    else:
        print(f"  ❌ FAIL: {test_name}")
        FAIL += 1

async def test_websocket():
    global PASS, FAIL
    print("===== WebSocket 功能测试 =====")
    
    uri = f"{BASE_URL}/api/v1/ws?token={TOKEN}"
    
    try:
        async with websockets.connect(uri) as ws:
            print("  ✅ PASS: WebSocket 连接成功")
            PASS += 1
            
            # WS-002: 发送心跳
            await ws.send(json.dumps({"type": "heartbeat", "data": {}}))
            # 心跳不需要服务端响应验证
            
            # WS-003: 发送消息
            msg = json.dumps({
                "type": "send_message",
                "data": {
                    "conversation_id": 1,
                    "type": "text",
                    "content": "WebSocket测试消息"
                },
                "request_id": "test-001"
            })
            await ws.send(msg)
            
            # 等待 ACK
            try:
                resp = await asyncio.wait_for(ws.recv(), timeout=5)
                data = json.loads(resp)
                assert_result(
                    data.get("type") in ["ack", "new_message"],
                    "发送消息收到响应"
                )
            except asyncio.TimeoutError:
                assert_result(False, "发送消息超时")
            
    except websockets.exceptions.ConnectionClosed:
        print("  ❌ FAIL: WebSocket 连接被关闭")
        FAIL += 1
    except Exception as e:
        print(f"  ❌ FAIL: WebSocket 异常: {e}")
        FAIL += 1
    
    print(f"\n===== WebSocket 测试结果: {PASS} 通过, {FAIL} 失败 =====")

asyncio.run(test_websocket())
```

### 22.8 运行说明

```bash
# 1. 启动后端服务
cd qim-server && go run main.go &

# 2. 创建测试账号（如果不存在）
curl -X POST http://localhost:8080/api/v1/auth/register \
    -H "Content-Type: application/json" \
    -d '{"username":"admin","password":"123456","nickname":"管理员"}'

# 3. 获取 Token
export QIM_TOKEN=$(curl -s -X POST http://localhost:8080/api/v1/auth/login \
    -H "Content-Type: application/json" \
    -d '{"username":"admin","password":"123456"}' | \
    python3 -c "import sys,json;print(json.load(sys.stdin)['data']['token'])")

export QIM_BASE_URL=http://localhost:8080

# 4. 运行完整回归测试
bash test-all.sh

# 5. 单独运行模块测试
bash test-auth.sh
bash test-users.sh

# 6. WebSocket 测试（需安装 websockets: pip3 install websockets）
python3 test-websocket.py
```

---

## 附录

### A. 测试优先级说明

| 优先级 | 说明 | 执行频率 |
|--------|------|----------|
| P0 | 核心功能，阻塞性问题 | 每次发布前必测 |
| P1 | 重要功能，影响用户体验 | 每周回归 |
| P2 | 次要功能，边界场景 | 每月回归 |

### B. 自动化状态说明

| 状态 | 说明 | 推荐工具 |
|------|------|----------|
| ✅ 可自动化 | 可直接通过 API/脚本执行 | curl + bash, Python |
| ⚠️ 需 E2E 工具 | 需要浏览器自动化 | Playwright, Cypress, Spectron |

### C. 推荐 E2E 工具

对于 UI 测试部分，推荐使用以下工具实现完全自动化：

- **Playwright** - 支持 Chromium/Firefox/WebKit，适合前端 Web 测试
- **Spectron** - Electron 专用测试框架
- **Cypress** - 现代 Web E2E 测试工具

### D. 测试数据清理

```bash
# 清理测试数据（慎用！）
# 删除 SQLite 数据库重新开始
rm qim-server/qim.db
rm -rf qim-server/uploads/*

# 重启服务创建干净环境
cd qim-server && go run main.go
```

### E. 持续集成建议

可将自动化测试脚本集成到 CI/CD 流程：

```yaml
# .github/workflows/regression-test.yml
name: Regression Test
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'
      
      - name: Start Server
        run: |
          cd qim-server
          go run main.go &
          sleep 5
      
      - name: Create Admin User
        run: |
          curl -X POST http://localhost:8080/api/v1/auth/register \
            -H "Content-Type: application/json" \
            -d '{"username":"admin","password":"123456","nickname":"管理员"}'
      
      - name: Run Regression Tests
        run: |
          export QIM_BASE_URL=http://localhost:8080
          export QIM_TOKEN=$(curl -s -X POST $QIM_BASE_URL/api/v1/auth/login \
            -H "Content-Type: application/json" \
            -d '{"username":"admin","password":"123456"}' | \
            python3 -c "import sys,json;print(json.load(sys.stdin)['data']['token'])")
          bash test-all.sh
```
