import type { ReactElement } from 'react';
import './app.less';
export interface Display {
    id: number;
    x: number;
    y: number;
    width: number;
    height: number;
}
export default function App(): ReactElement;
